# 如何部署你的内容到这个网站

这是一个基于 Jekyll 的学术个人网站模板。以下是详细的部署步骤：

## 📋 目录
1. [基本配置](#基本配置)
2. [添加内容](#添加内容)
3. [本地预览](#本地预览)
4. [部署到 GitHub Pages](#部署到-github-pages)

---

## 基本配置

### 1. 修改网站基本信息

编辑 `_config.yml` 文件，更新以下内容：

```yaml
# 网站标题和描述
title: "你的名字 / 网站标题"
name: "你的名字"
description: "你的个人学术作品集"

# 网站URL（如果使用GitHub Pages）
url: https://你的用户名.github.io
baseurl: ""  # 如果仓库名不是 username.github.io，则填写仓库名

# 作者信息
author:
  name: "你的侧边栏名字"
  bio: "左侧边栏的简短个人简介"
  location: "你的位置"
  employer: "你的单位"
  email: "your.email@example.com"
  avatar: "profile.png"  # 头像图片放在 images/ 目录下
  
  # 社交媒体链接（根据需要填写）
  github: "你的GitHub用户名"
  twitter: "你的Twitter用户名"
  linkedin: "你的LinkedIn用户名"
  # ... 其他社交媒体
```

### 2. 更新导航菜单

编辑 `_data/navigation.yml` 文件，可以：
- 调整菜单顺序
- 删除不需要的菜单项（如没有作品集就删除 Portfolio）
- 添加自定义链接

---

## 添加内容

### 📝 添加博客文章

在 `_posts/` 目录下创建新的 Markdown 文件，文件名格式：`YYYY-MM-DD-文章标题.md`

**示例：**
```markdown
---
title: '我的第一篇博客文章'
date: 2024-01-15
permalink: /posts/2024/01/my-first-post/
tags:
  - 标签1
  - 标签2
---

这是文章内容，支持 Markdown 格式。

## 二级标题

你可以使用各种 Markdown 语法。
```

### 📚 添加出版物

在 `_publications/` 目录下创建文件，文件名格式：`YYYY-MM-DD-论文标题.md`

**示例：**
```markdown
---
title: "我的论文标题"
collection: publications
category: manuscripts  # manuscripts, conferences, books
permalink: /publication/2024-01-15-my-paper
excerpt: '这是论文的简短摘要。'
date: 2024-01-15
venue: '期刊或会议名称'
paperurl: 'https://example.com/paper.pdf'  # 或 '/files/paper.pdf'
citation: '你的名字, 等. (2024). "论文标题." <i>期刊名</i>.'
---

这里是论文的详细描述。
```

### 🎤 添加演讲/报告

在 `_talks/` 目录下创建文件，文件名格式：`YYYY-MM-DD-演讲标题.md`

**示例：**
```markdown
---
title: "我的演讲标题"
collection: talks
type: "Talk"
permalink: /talks/2024-01-15-my-talk
venue: "会议或机构名称"
date: 2024-01-15
location: "城市, 国家"
---

演讲的详细内容。
```

### 📖 添加教学课程

在 `_teaching/` 目录下创建文件，文件名格式：`YYYY-学期-课程名.md`

**示例：**
```markdown
---
title: "课程名称"
collection: teaching
type: "Undergraduate course"
permalink: /teaching/2024-spring-course
venue: "大学名称"
date: 2024-01-15
---

课程描述和内容。
```

### 💼 添加作品集项目

在 `_portfolio/` 目录下创建文件，可以是 `.md` 或 `.html` 格式。

**示例：**
```markdown
---
title: "项目名称"
collection: portfolio
image: /images/project-image.png
---

项目描述和详情。
```

### 📄 修改关于页面

编辑 `_pages/about.md` 文件，更新你的个人介绍。

---

## 本地预览

在部署到 GitHub 之前，建议先在本地预览网站。

### 方法1：使用 Docker（推荐）

```bash
# 在项目根目录执行
chmod -R 777 .
docker compose up
```

然后在浏览器访问 `http://localhost:4000`

### 方法2：使用本地 Jekyll

```bash
# 安装依赖（MacOS）
brew install ruby
brew install node
gem install bundler

# 安装项目依赖
bundle install

# 启动本地服务器
bundle exec jekyll serve -l -H localhost
```

访问 `http://localhost:4000` 查看网站。

---

## 部署到 GitHub Pages

### 步骤1：创建 GitHub 仓库

1. 在 GitHub 上创建一个新仓库
2. 仓库名建议使用：`你的用户名.github.io`（这样网站URL就是 `https://你的用户名.github.io`）
3. 如果使用其他仓库名，需要在 `_config.yml` 中设置 `baseurl: "/仓库名"`

### 步骤2：上传代码

```bash
# 初始化 git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤3：启用 GitHub Pages

1. 进入仓库的 Settings（设置）
2. 找到 Pages 选项
3. 在 Source 中选择 `main` 分支（或 `master` 分支）
4. 点击 Save

等待几分钟，你的网站就会在 `https://你的用户名.github.io` 上线了！

### 步骤4：后续更新

每次修改内容后：

```bash
git add .
git commit -m "更新内容描述"
git push
```

GitHub Pages 会自动重新构建网站（通常需要几分钟）。

---

## 其他提示

### 添加图片

- 将图片放在 `images/` 目录下
- 在 Markdown 中使用：`![图片描述](/images/图片名.png)`

### 添加 PDF 文件

- 将 PDF 放在 `files/` 目录下
- 在内容中链接：`[下载PDF](/files/文件名.pdf)`

### 自定义样式

- 编辑 `_sass/` 目录下的 SCSS 文件
- 或修改 `assets/css/main.scss`

### 更新头像

- 将头像图片放在 `images/` 目录
- 在 `_config.yml` 中设置 `avatar: "你的头像文件名.png"`

---

## 常见问题

**Q: 网站没有更新？**
A: GitHub Pages 构建需要几分钟，检查仓库的 Actions 标签查看构建状态。

**Q: 如何删除示例内容？**
A: 删除 `_posts/`、`_publications/`、`_talks/` 等目录下的示例文件即可。

**Q: 如何修改主题颜色？**
A: 编辑 `_sass/theme/` 目录下的 SCSS 文件。

**Q: 支持中文吗？**
A: 完全支持！所有内容都可以使用中文。

---

## 更多资源

- [Jekyll 官方文档](https://jekyllrb.com/)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [GitHub Pages 文档](https://docs.github.com/pages)

祝你部署顺利！🎉

